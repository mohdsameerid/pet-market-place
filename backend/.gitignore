# Build output
**/bin/
**/obj/

# Visual Studio
.vs/
*.user

# App settings — only the base template (no real values) is committed.
# Real credentials go in .NET User Secrets (dotnet user-secrets) or environment variables.
/appsettings.*.json
!/appsettings.json
.env

# EF Core migrations (generated)
Migrations/

# Publish output
publish/


appsettings.Production.json
**/bin/
**/obj/